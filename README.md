# bPanel Org

The website for bPanel

## Getting started
Install:
``` bash
$ npm install hexo-cli -g
$ cd [wherever you cloned this]
$ npm install
$ hexo generate
$ hexo server
```

Generate:

``` bash
$ hexo generate
```

Run server:

``` bash
$ hexo server
```
