# bPanel Org

The website for bPanel

## Getting started
Install:
``` bash
$ npm install hexo-cli -g
$ cd bpanel-org
$ npm install
$ hexo generate
$ hexo server
```

Generate:

``` bash
$ hexo generate
```

Run server:

``` bash
$ hexo server
```
